# Google Drive Configuration
# Méthode 1 : Service Account (Recommandé pour production)
# Get these from Google Cloud Console > Service Accounts
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# Méthode 2 : OAuth (Alternative si Service Account ne fonctionne pas)
# Get these from Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-oauth-client-secret
GOOGLE_REFRESH_TOKEN=your-oauth-refresh-token
# Redirect URI (optionnel, auto-détecté en production si NEXT_PUBLIC_APP_URL est défini)
GOOGLE_REDIRECT_URI=http://localhost:1001

# Admin Authentication
# Generate a password hash using: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10).then(console.log)"
ADMIN_PASSWORD_HASH=your-bcrypt-hash-here
ADMIN_SESSION_SECRET=your-random-secret-key-here

# Next.js Configuration
NODE_ENV=production
# URL de l'application en production (utilisée pour OAuth redirect URI)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Supabase Configuration
# Get these from Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

